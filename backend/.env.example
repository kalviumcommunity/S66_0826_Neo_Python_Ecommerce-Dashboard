# Database connection URL (supports PostgreSQL or SQLite)
# Example PostgreSQL (Neon / Supabase):
# DATABASE_URL="postgresql://username:password@ep-xyz.us-east-2.aws.neon.tech/neondb?sslmode=require"
# Example SQLite (local):
# DATABASE_URL="sqlite:///server/analytics.db"
